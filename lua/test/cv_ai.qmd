# Create a test .qmd file and add this code block
```{python}
import numpy as np
import matplotlib.pyplot as plt

# Generate sample data
x = np.linspace(0, 2*np.pi, 100)
y = np.sin(x)

print("Data generated successfully!")
print(f"X shape: {x.shape}, Y shape: {y.shape}")

```
